from adding import agents
# print(agents)

#removing can be done in 2 ways:
  #using dictionary.pop(key); O(1)
  #del dictionary[key] ; O(1); wouldn't use this del method tho... tb continued

agents.pop("retake_generator")


print(agents)
